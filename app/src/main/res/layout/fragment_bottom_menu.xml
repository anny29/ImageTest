<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="30dp"
    android:layout_marginTop="10dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:orientation="horizontal"
    android:background="@color/colorBottomMenuBlue"
    android:showDividers="beginning"
    android:divider="@drawable/divider_line"
    >

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/bottom_notice"
        style="@style/BottomMenuFontStyle"
        android:text="@string/bottom_notice" />

    <TextView
        android:id="@+id/bottom_factor"
        style="@style/BottomMenuFontStyle"
        android:text="@string/bottom_factor" />

    <TextView
        android:id="@+id/bottom_person_info"
        style="@style/BottomMenuFontStyle"
        android:text="@string/bottom_person_info" />

</LinearLayout>
